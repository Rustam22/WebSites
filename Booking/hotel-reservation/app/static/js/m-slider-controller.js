function MSliderController(identifier) {		var self, slider = {};		init();		function init() {		self = $(identifier);		slider.items = self.find('.mslider-item');		slider.slidesCount = slider.items.length;				var segments = tmpl($('#mslider-segment-template').html(), {slidesCount : slider.slidesCount});		self.find('.mslider-top').prepend(segments);				DragMaster.onDrag(function(){console.log(10);}).makeDraggable(self.find('.mslider-pointer'));	}				function onResize() {			}			}